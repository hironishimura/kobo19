<?php
/**
 * 最初に入るアプリ（SujiCalc）の内容。
 *
 * テーマを有効化したときに、この内容で「アプリ」が1件作られます。
 * 入れたあとは管理画面から編集してください。
 *
 * @package kobo19
 */

defined( 'ABSPATH' ) || exit;

/**
 * 最初に登録するアプリを返す。
 *
 * @return array<string, mixed>
 */
function kobo19_app_source() {
	return array(
		'slug'    => 'sujicalc',
		'title'   => 'SujiCalc',
		'excerpt' => '打つと右に答えが出る、ノート型の電卓。iPhone・iPad・Mac 対応。',
		'meta'    => array(
			'tagline'  => '打つと右に答えが出る、ノート型の電卓。',
			'lead'     => "式を打つと、その行の右側にすぐ答えが出ます。「＝」は要りません。\n書いた式はそのまま残るので、あとから数字を直せば、続きの計算も一度に合い直ります。",
			'store'    => '',
			'version'  => '1.0',
			'requires' => 'iOS 17 / iPadOS 17 / macOS 14 以降',
			'category' => '仕事効率化',
			'price'    => '',
			'status'   => '準備中',
			'demo'     => "[calc label=\"打つと、右に出る\"]\n// 8月分の見積もり |\n単価 = 1200円 | 1,200円\n個数 = 35 | 35\n単価 * 個数 | 42,000円\n+ 10% | 46,200円\n \n梁幅 = 120 mm | 120 mm\n梁せい = 300 mm | 300 mm\n梁幅 * 梁せい | 36,000 mm²\n[/calc]",
		),
		'content' => <<<'TEXT'
## 単位がそのまま通る

数字のうしろに単位を書くだけです。掛け算・割り算をすると `mm²` や `N/mm²` のように組み立てられ、種類の違う単位を足そうとするとエラーで止まります。

[calc numbers="no"]
120 mm * 300 mm | 36,000 mm²
36 kN / 36000 mm2 | 1 N/mm²
10 km in mile | 6.2137119224 mile
[/calc]

## 名前を付けて使い回す

値に名前を付けると、ほかの行から呼び出せます。名前を変えれば、その名前を使っている行もまとめて書き換わります。前の行は `prev`、3行目は `#3`、空行までの合計は `sum` です。

[calc numbers="no"]
梁幅 = 120 mm | 120 mm
梁せい = 300 mm | 300 mm
梁幅 * 梁せい | 36,000 mm²
[/calc]

## 表にしてまとめて集計

値を並べて持てます。`sum` `avg` `min` `max` `sort` で扱え、文字も混ぜられるので、計算書の見出しを付けられます。

[calc numbers="no"]
価格 = [4800, 12000, 3200] | [4,800, 12,000, 3,200]
sum(価格) | 20,000
avg(価格) | 6,666.6666666667
[/calc]

## グラフになる

`plot` と書けば曲線が、`chart` と書けば棒グラフが本文の下に出ます。打ち直すたびにその場で描き直されます。

[calc numbers="no"]
plot(x^2 - 3x) |
chart(家賃, 光熱費, 食費) |
[/calc]

## かんたんなプログラム

`if` `for` `while` `def` が Python と同じ書き方で使えます。繰り返しの計算や、自分用の関数を作れます。作った関数はほかのシートからも呼べます。

[calc numbers="no"]
def 税込(x): |
    return x * 1.1 |
税込(4800) | 5,280
[/calc]

## 3つの端末で同じ書類

iPhone・iPad・Mac の書類は iCloud で自動的に同期します。現場の iPhone に打ち込んだ数字を、事務所の Mac でそのまま続けられます。別々のシートを編集していれば、どちらも残ります。

## 何も集めません

広告も、利用状況の解析も入っていません。そもそも、外部と通信する機能がアプリに含まれていません。書いた内容は、お使いの端末とご自身の iCloud にだけ保存されます。
TEXT
,
	);
}
